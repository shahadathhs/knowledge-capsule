pre-commit:
  parallel: true
  commands:
    fmt:
      run: go fmt ./...
    vet:
      run: go vet ./...
    tidy:
      run: go mod tidy

pre-push:
  parallel: true
  commands:
    build-local:
      run: ./scripts/build-local.sh
    # build-docker:
    #   run: ./scripts/build-docker.sh
      